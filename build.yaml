# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command and `artifact-name` to assign a name to
# distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     cmake-args: -DCONFIG_ZMK_USB_LOGGING=y
#     artifact-name: corne_left_with_logging
#

jobs:
  build:
    strategy:
      matrix:
        include:
          - board: seeeduino_xiao_ble
          # shield: FrostOrtho_R rgbled_adapter # zmk-rgbled-widgetを使用する場合はこっちを使う
            shield: FrostOrtho_R
            snippet: studio-rpc-usb-uart
            cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

          - board: seeeduino_xiao_ble
          # shield: FrostOrtho_L rgbled_adapter # zmk-rgbled-widgetを使用する場合はこっちを使う
            shield: FrostOrtho_L
            cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

          - board: seeeduino_xiao_ble
            shield: settings_reset
          # --- ② ドングル構成 (Dongle = Central) ---
            # ドングル本体 (Prospector)
          - board: seeeduino_xiao_ble
            shield: FrostOrtho_dongle
            artifact-name: FrostOrtho_prospector_dongle

          # ドングル用の左手側 (Peripheral)
          - board: seeeduino_xiao_ble
            shield: FrostOrtho_L
            cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
            artifact-name: FrostOrtho_L_for_dongle

          # ドングル用の右手側 (Peripheral / トラックボール側)
          - board: seeeduino_xiao_ble
            shield: FrostOrtho_R
            cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
            artifact-name: FrostOrtho_R_for_dongle
